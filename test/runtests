#!/bin/bash

TIMEFORMAT=$' (%2lR real)'

tests="hilbert irrdecom frob intersect assoprimes minimize transform ptransform latformat alexdual radical"
for t in $tests;
do
  echo -n "$t "
  cd test/$t
  ./runtests $*
  if [ $? != 0 ]; then exit 1; fi
  echo
  cd ../..
done
